# PostgreSQL Database Credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=josephmuli
POSTGRES_DB=mlinzi
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# SQLModel/SQLAlchemy Database Connection URL
# This is used by FastAPI and Alembic to connect locally
DATABASE_URL=postgresql://postgres:josephmuli@localhost:5432/mlinzi
